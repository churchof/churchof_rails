<%= simple_form_for @match_campaign do |f| %>
  <% if @match_campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match_campaign.errors.count, "error") %> prohibited this expense from being saved:</h2>
      <ul>
      <% @match_campaign.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="field-pair">
        <%= f.input :start_date %>
      </div>
      <div class="field-pair">
        <%= f.input :end_date %>
      </div>
      Right now this could be edited...
      <div class="field-pair">
        <%= f.input :initial_amount %>
      </div>
      <div class="field-pair">
        <%= f.input :organization_id, collection: Organization.all.order('title'), label_method: :title %>
      </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
