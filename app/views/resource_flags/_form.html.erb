<%= simple_form_for [@resource, @resource_flag] do |f| %>
  <% if @resource_flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_flag.errors.count, "error") %> prohibited this expense from being saved:</h2>
      <ul>
      <% @resource_flag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      <%= f.input :user_id_church_admin, :as => :hidden, :input_html => { :value => current_user.id } %>
      <div class="field-pair">
        <%= f.input :flagged, :label => 'Flagged?', :input_html => { :checked => true } %>
      </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>