<%= simple_form_for @organization_role do |f| %>
  <% if @organization_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization_role.errors.count, "error") %> prohibited this expense from being saved:</h2>
      <ul>
      <% @organization_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="field-pair">
        <%= f.input :organization_id, label: "Organization", collection: Organization.all, label_method: :title %>
      </div>

      <div class="field-pair">
        <%= f.input :user_id, label: "User", collection: User.with_role(:resource_partner), label_method: :full_name %>
      </div>


        <%= f.input :role_type %>

        Note that currently all roles besides resource manager simply link a person to an org to show on the profile that way... in the future this will actually give them that permission. Shouldnt be able to give the type of church admin to a non-church org... etc... this needs added to github also... so these are dumb associations for connection only... will need rehashing inorder to actually provide abilities.

        <%= f.input :pending, :as => :hidden, :input_html => { :value => false } %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
