<% @needs_supported.each do |need| %>
	<%= need.title_public %>
	<br>
	<% Contribution.where(user:current_user, need:need).each do |contribution| %>
		<%= contribution.amount %> | <%= time_ago_in_words(contribution.created_at) %>
		<br>
	<% end %>
	<br>
	<br>
<% end %>