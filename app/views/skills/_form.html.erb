<%= simple_form_for @skill do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this expense from being saved:</h2>
      <ul>
      <% @skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="field-pair">
        <%= f.input :name %>
      </div>
      <div class="field-pair">
        <%= f.input :description %>
      </div>



      <div class="field-pair">
        <%= f.input :initiative_id, label: "Initiative", collection: Initiative.all, label_method: :title %>



      </div>




  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
