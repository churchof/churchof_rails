<%= simple_form_for @organization do |f| %>
  <% if @organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization.errors.count, "error") %> prohibited this expense from being saved:</h2>
      <ul>
      <% @organization.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="field-pair">
        <%= f.input :title %>
      </div>
      <div class="field-pair">
        <%= f.input :description %>
      </div>
      <div class="field-pair">
        <%= f.input :address %>
      </div>
      <div class="field-pair">
        <%= f.input :organization_type %>
      </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
